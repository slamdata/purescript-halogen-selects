## Module Halogen.Components.Select.Utils.Array

#### `repeat`

``` purescript
repeat :: forall a. Int -> Array a -> Array a
```

#### `shift`

``` purescript
shift :: forall a. Int -> Array a -> Array a
```


